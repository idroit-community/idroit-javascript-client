/*
 * iDroit Dashboard Admin API
 *      これは[株式会社フォアー](https://www.fore-co.ltd/ja/)が開発するを使ったDecentralizd Identifiers / Verifiable Credentials(DID/VC)に関係する機能を簡単に利用するための REST API です。現在以下のユースケースをサポートしています。これは今後も拡張されていきます。     - DIDの生成:      - グループ管理機能       - (企業/プロジェクトのまとまり)ごとにユーザー、クライアント、証明書(VC)スキーマを紐付けて管理する。          詳細は以下を参照してください。     - [idroit dashboard admin UI](https://admin.idroit-dashboard.com)     - [idroit dashboard公式ホームページ]()     - [idroit dashboard操作マニュアル]()      以下は関連リンクです。     - [Universal Resolver](https://dev.uniresolver.io/)     - [W3C DID Core 1.0](https://www.w3.org/TR/did-core/)     - [Verifiable Credentials Data Model v2.0](https://www.w3.org/TR/vc-data-model-2.0/)   
 *
 * OpenAPI spec version: 0.9.2
 *
 * NOTE: This class is auto generated by the swagger code generator program.
 * https://github.com/swagger-api/swagger-codegen.git
 *
 * Swagger Codegen version: 3.0.68
 *
 * Do not edit the class manually.
 *
 */
import ApiClient from '../ApiClient';

/**
 * The SendMailDto model module.
 * @module model/SendMailDto
 * @version 0.9.2
 */
export default class SendMailDto {
  /**
   * Constructs a new <code>SendMailDto</code>.
   * @alias module:model/SendMailDto
   * @class
   * @param title {String} (必須) メールの件名
   * @param content {String} (必須) メールの内容
   */
  constructor(title, content) {
    this.title = title;
    this.content = content;
  }

  /**
   * Constructs a <code>SendMailDto</code> from a plain JavaScript object, optionally creating a new instance.
   * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
   * @param {Object} data The plain JavaScript object bearing properties of interest.
   * @param {module:model/SendMailDto} obj Optional instance to populate.
   * @return {module:model/SendMailDto} The populated <code>SendMailDto</code> instance.
   */
  static constructFromObject(data, obj) {
    if (data) {
      obj = obj || new SendMailDto();
      if (data.hasOwnProperty('title'))
        obj.title = ApiClient.convertToType(data['title'], 'String');
      if (data.hasOwnProperty('content'))
        obj.content = ApiClient.convertToType(data['content'], 'String');
      if (data.hasOwnProperty('userId'))
        obj.userId = ApiClient.convertToType(data['userId'], 'String');
    }
    return obj;
  }
}

/**
 * (必須) メールの件名
 * @member {String} title
 */
SendMailDto.prototype.title = undefined;

/**
 * (必須) メールの内容
 * @member {String} content
 */
SendMailDto.prototype.content = undefined;

/**
 * (任意) メールの送信対象のユーザーアカウント識別子
 * @member {String} userId
 */
SendMailDto.prototype.userId = undefined;

